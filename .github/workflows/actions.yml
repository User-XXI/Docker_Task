name: Task
on:
  push:
  
jobs:
  Build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: build
          shell: bash
          run: |
            docker build -t logger .
            docker images 
            docker inspect logger
            docker inspect logger 
